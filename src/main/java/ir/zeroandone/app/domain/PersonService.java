/*package ir.zeroandone.app.domain;

public class PersonService {
    {
            ServicePointManager.ServerCertificateValidationCallback =
                    (s, certificate, chain, sslPolicyErrors) => true;
            var client = new SoapSmsQueuableImplementationClient("TSOAP");
            client.ClientCredentials.UserName.UserName = "dadgar_1395";
            client.ClientCredentials.UserName.Password = "mfB%g@b1M1H6X2#U";
            var msg = client.enqueue("magfa", new[] { message.Body }, new[] { message.Destination }, new[] { "+98300081395" }, null, null, new[] { 1 }, null, null);
            // Plug in your SMS service here to send a text message.
            return Task.FromResult(0);
    }
}*/
